name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v1.2.0) — must already exist on the repo'
        required: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: FlatSQL-Studio-Windows-Setup.exe
          - os: macos-latest
            artifact: FlatSQL-Studio-macOS.zip

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "pyinstaller>=6.10" "pyinstaller-hooks-contrib>=2025.6"
          pip install -r requirements.txt
          pip list

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          pyinstaller \
            --noconfirm \
            --clean \
            --windowed \
            --name "FlatSQL-Studio" \
            --icon "src/flatsql/assets/img/icon.ico" \
            --add-data "src/flatsql/assets;assets" \
            --paths src \
            --collect-all sqlfluff \
            --collect-all pyarrow \
            --collect-all numpy \
            --copy-metadata numpy \
            --copy-metadata pyarrow \
            --collect-submodules duckdb \
            run.py

      - name: Verify numpy is fully bundled (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          echo "=== dist/FlatSQL-Studio/_internal/numpy/_core (must contain _multiarray_umath.*.pyd) ==="
          ls dist/FlatSQL-Studio/_internal/numpy/_core/ 2>/dev/null | head -40 || echo "MISSING numpy/_core directory"
          echo ""
          echo "=== PyInstaller warnings (top 80 lines) ==="
          if [ -f "build/FlatSQL-Studio/warn-FlatSQL-Studio.txt" ]; then
            head -80 "build/FlatSQL-Studio/warn-FlatSQL-Studio.txt"
          else
            echo "no warn file"
          fi

      - name: Build (macOS)
        if: runner.os == 'macOS'
        run: |
          pyinstaller \
            --noconfirm \
            --clean \
            --windowed \
            --name "FlatSQL Studio" \
            --add-data "src/flatsql/assets:assets" \
            --paths src \
            --collect-all sqlfluff \
            --collect-all pyarrow \
            --collect-all numpy \
            --copy-metadata numpy \
            --copy-metadata pyarrow \
            --collect-submodules duckdb \
            run.py

      - name: Build Windows installer (Inno Setup)
        if: runner.os == 'Windows'
        shell: cmd
        run: '"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" packaging\windows\FlatSQL-Studio.iss'

      - name: Collect Windows artifact
        if: runner.os == 'Windows'
        shell: bash
        run: mv "packaging/windows/Output/FlatSQL-Studio-Setup.exe" "FlatSQL-Studio-Windows-Setup.exe"

      - name: Collect macOS artifact
        if: runner.os == 'macOS'
        run: |
          cd dist && zip -r "../FlatSQL-Studio-macOS.zip" "FlatSQL Studio.app"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          files: |
            FlatSQL-Studio-Windows-Setup.exe
            FlatSQL-Studio-macOS.zip
          generate_release_notes: true
